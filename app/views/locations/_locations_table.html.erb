<%= render 'shared/search_navbar_top', models: @locations %>
<%= render 'locations', user: @user, sticker: @sticker, locations: @locations %>
<%= render 'shared/search_navbar_bottom', models: @locations %>

