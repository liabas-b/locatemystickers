<% complement ||= "" %>

<%= render 'shared/search_navbar_top', models: locations, complement: complement %>
<div id="<%= complement %>locations" class="row-fluid">
  <%= render 'locations', locations: locations %>
</div>
<%= render 'shared/search_navbar_bottom', models: locations, complement: complement %>

